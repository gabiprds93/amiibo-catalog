.Categories
  padding-top: $size-header-height;

  &__header
    background: $color-white;
    height: $size-header-height;
    left: 0;
    position: fixed;
    right: 0;
    top: 0;

  &__main
    padding-top: $size-header-height;